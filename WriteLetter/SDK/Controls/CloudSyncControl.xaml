<UserControl
    x:Class="AppCore.SDK.Controls.CloudSyncControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppCore.SDK.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Command="{Binding CloudButtonPressCommand}">
            <TextBlock>Could</TextBlock>
        </Button>
        <Button Grid.Row="1" Command="{Binding UpLoadItemCommand}">
            <TextBlock Text="up"/>
        </Button>
        <Button Grid.Row="2" Command="{Binding DownLoadItemCommand}">
            <TextBlock Text="Down"/>
        </Button>
    </Grid>
</UserControl>
